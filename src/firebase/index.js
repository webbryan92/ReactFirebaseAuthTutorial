import * as auth from './auth';
import * as db from './db';
import * as firebase from './firebase';

export {
  auth,
  db,
  firebase,
};

//this file acts as an interface as the sole entry to the firebase modules